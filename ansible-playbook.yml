---
- name: Pull docker image of Calculator
  hosts: all
  tasks:
    - name: Start docker service
      service:
        name: docker
        state: started

    - name: pull docker image
      shell: docker pull um86/nuptials2
    - name: running container
      shell: docker run -it -d um86/nuptials2 /bin/bash
